services:
  cb2api:
    build: .
    container_name: codebuddy2api
    restart: unless-stopped
    ports:
      - "7865:7865"
    volumes:
      - ./auths:/app/auths
      - ./data:/app/data
      - ./config.json:/app/config.json:ro
