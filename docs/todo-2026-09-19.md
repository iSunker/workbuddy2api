# 待办：三个问题（2026-09-19）

来源：NAS 端 wb2api 使用中出现的现象 + 两个追问。以下按「现象 → 证据 → 结论 → 待办」记录。

---

## 待办 1 — `/compact` 报 11148 `tool_call_sequence_broken`

**现象**
```
Error during compaction: API Error: 400 upstream client (http 400):
{"code":11148,"msg":"tool calls and tool results do not match, please start a new conversation and retry",
 "extError":{"code":"tool_call_sequence_broken","message":"tool calls and tool results do not match, ..."}}
```

**证据（本地代码 + 实测数据）**
- `grep 11148` 全仓 **零命中**：`internal/upstream/classify.go` 只处理 11128 / 11102（模型通道）、
  11140（内容审核），11148 **未被识别**。
- 400 → `Classify()` 落到 `if status >= 400 { ... return ErrClient }`（classify.go:133-139），
  与 11128 同档处理。
- `handler.go` 的 `ErrClient + IsModelChannelRejected` 分支会**换账号重试**；而
  `IsModelChannelRejected` 靠 `e.Code`（11128/11102）或关键词判定，**11148 两条都不命中**
  （classify.go:192-209）→ 直接 400 甩给客户端。
- 本地 `data/usage.json`：最大单次 `total_tokens = 559,405`，>200k 的有 17 次，
  >100k 的 84 次（中位数 6 万）。说明**超长上下文是常态**，断链更容易暴露。

**结论（对用户推测的修正）**
用户推测「某次工具调用被打断 / 手动删了历史消息」——**部分正确但不完整**。真实机制分两层：

1. **上游是强校验**：要求每个 assistant 的 `tool_calls` 后紧跟配对的 `tool` 结果，ID 必须一一对应。
   `/compact` 会把**全量历史**发上去，任何一处不配对就整条拒绝。
2. **本网关会主动制造不配对**——这才是关键，且是"必然踩到"而非偶发：
   - `sse.go` 的 `IsEmptyToolResponse` 认为「空 tool_calls 响应」不可用 → 触发**换号重试**；
   - 但**出错的那一轮 assistant 消息（含 tool_calls）已经被流式写进客户端历史**，
     重试得到的第二条 assistant 消息（可能没有 tool_calls）也已写入；
   - `sse.go:307-316` 的 `renameToolCallID` 把 `tool_call.id` 改写成 `toolu_` 前缀，
     于是**重试前后生成的 tool_call id 不同**，配对的 tool 结果只能对上其中一个；
   - 历史里就留下了「孤儿 tool_call」或「孤儿 tool result」→ 11148。
3. **次因**：CC Switch 模式下 Claude Code 以为上下文还有余量，很少主动压缩；一旦 `/compact`
   把 559k tokens 全量上送，历史里早先累积的断点集中爆发。

**处置（上游已明确给出）**
- 立刻可用：`/clear` 或 `claude -c` 之外新建会话——上游原话
  *"please start a new conversation and retry"*。
- 折中：`claude --resume <id> --fork-session` 从历史分叉，再让它尽早压缩。
- **不建议**手工编辑 `~/.claude/projects/**/*.jsonl` 删消息——配对是链式的，删一条会造出新的断点。

**代码侧待办（三选一或组合）**
- [ ] `modelChannelBlockedCodes` 加 `11148`，或新增 `IsBrokenToolSequence(e)` 判定，
      命中时**换账号重试**而不是 400（换号能拿到一份干净的上下文？——需实测确认，因为
      断链在请求体里，换号大概率一样 400，**所以这条要先验证再动手**）。
- [ ] 更可能有效的方向：`IsEmptyToolResponse` 换号重试时，**保证重试轮不把两条 assistant
      消息都写进历史**；或重试时沿用同一条 `tool_call.id`（不在重试轮二次 `renameToolCallID`）。
- [ ] 给 11148 一个专门的 `ErrKind`（如 `ErrBrokenToolSeq`），映射成**明确文案**，
      提示"请新建会话"，而不是把裸 400 透给客户端——至少让用户看得懂。

---

## 待办 2 — Claude Code 没有上下文进度条

**现象**：Claude CLI 底部不显示 context 占用（如 `12% used`）。

**证据（本机 `~/.claude/settings.json`）**
```json
"ANTHROPIC_BASE_URL": "http://127.0.0.1:15721",          ← 指向 CC Switch，不是网关
"CLAUDE_CODE_DISABLE_UNKNOWN_MODEL_WINDOW_ENFORCEMENT": "1",
"ANTHROPIC_DEFAULT_OPUS_MODEL": "claude-opus-5[1M]",
"ANTHROPIC_DEFAULT_OPUS_MODEL_NAME": "deepseek-v4.1-flash",
"CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1"
```
而 `claude-profiles/wb-local.example.json` 里映射的是 `deepseek-v4.1-flash[1M]`。

**结论**
上下文进度条 = **已用 token ÷ 模型窗口**。本网关的模型表虽然会回 `context_length`，
但上游模型名 `deepseek-v4.1-flash` **不在 Claude Code 内置的窗口表里**，于是分母取不到；
再叠加 `CLAUDE_CODE_DISABLE_UNKNOWN_MODEL_WINDOW_ENFORCEMENT=1`（该开关的本意就是
"未知模型不做窗口限制/不报错"），**分母缺失 → 进度条无处渲染**。

注意这是**客户端侧的显示问题，与网关能否服务无关**——所以两边表现不一致：
- 走本机 profile（`..._MODEL_NAME` = `deepseek-v4.1-flash`）→ 无进度条；
- 走 CC Switch（`..._OPUS_MODEL` = `claude-opus-5[1M]`，带 `[1M]` 后缀）→ 有进度条。

**待办**
- [ ] 在 profile 的 `ANTHROPIC_MODEL` 上加窗口后缀（如 `deepseek-v4.1-flash[1M]`）试试是否恢复；
      `[1M]` 是 Claude Code 的窗口标注语法，`claude-profiles/*.example.json` 里
      `ANTHROPIC_DEFAULT_*_MODEL` 已经这么写了，但 `ANTHROPIC_MODEL` 本身没带。
- [ ] 若无效，改用 `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` 让它按 `/v1/models` 的
      `context_length` 取值——需确认本网关 `/v1/models` 是否真的返回了 `context_length`
      （`listModels` 仅当 `m.ContextLen > 0` 才带该字段，而 `ContextLen` 取自上游
      `max_input_tokens` 等字段，**上游可能压根不给**）。
- [ ] 明确区分：这条**不是 bug**，是显示能力缺失；不要为它去改网关的协议层。

---

## 待办 3 — 安全：仓库里已跟踪真实 `config.json`

**现象**（上一轮阅读项目时发现，用户未要求但顺带记录）
- `config.json` 含真实 `api_key`，且**已被 git 跟踪** → `.gitignore` 里的 `config.json`
  规则对它无效，历史提交里已有该 key。
- 同类问题：`/admin` 无鉴权且把网关 key 明文注入页面。

**待办**
- [x] ~~`git rm --cached config.json`，轮换该 api_key。~~ → 核实后发现 config.json **并未被跟踪**，无需处理（详见文末执行记录）。
- [ ] 可选：给 `/admin` 加一层鉴权。

---

## 执行记录（2026-09-19 第二轮）

### ✅ 已完成 — 待办 1：11148 文案

按「先修文案、不动重试策略」的既定顺序实施（**换号重试那条仍待实测**）：

- `classify.go` 新增 `ErrBrokenToolSeq` 类别 + `brokenToolSeqCode = 11148` 常量 +
  `String()` 返回 `"broken_tool_sequence"`。
- `Classify()` 中**优先于 `hardMarkers`** 判定 11148——否则错误文案里的
  "do not match" 之类措辞可能被 `hardMarkers` 抢先判成 `ErrHardCredit` 而误冷却 12h。
  判据：`code == 11148` **或** body 含 `tool_call_sequence_broken`。
- `handler.go` 的 `forwardStream` switch 新增分支：**直接交回调用方**，
  不换号、不冷却、不 NoteError（断链在请求体里，这些动作全是误伤）。
- `statusForKind()` 映射为 **400**（而非 429/503）——避免诱导客户端「稍后重试」，
  因为重试永远不会成功。
- 新增 `hintBrokenToolSeq()`：给上游英文原文补一段中文可操作提示
  （`/clear` 新建会话 / `--fork-session` 分叉）。
- 回归测试：`TestClassify` 增加 2 个用例（完整 11148 报文 + 仅 marker），**已通过**。

验证：`go build ./...` / `go vet ./...` / `go test ./...` 全绿。

### ⚠️ 待办 2 的实际结论：补 `[1M]` **修不好进度条**，且我已改错了一次

**用户指出的关键事实**：WorkBuddy 客户端里 `deepseek-v4.1-flash` 是**让用户自己选** 300k 或 1M 的。
这意味着 `[1M]` 不是模型的固有属性，而是**用户档位**——我直接写死 `[1M]` 是没有依据的。

#### 实测一：上游不给任何窗口信息

```
$ curl /v1/models        # 经网关
models: 19
any with context_length: 0      ← 19 个模型，一个都没有
any with cost_factor:    12     ← 特征：返回的是静态兜底表

$ 直连上游（绕开网关过滤）
GET https://copilot.tencent.com/v2/models  → HTTP 404 {"error_msg":"404 Route Not Found"}
GET https://www.codebuddy.ai/v2/models     → HTTP 404 404 page not found
GET /v3/config                             → 只有 enterpriseId / productFeatures，无模型窗口
```

`models.go:161` 从 `max_input_tokens` / `context_length` 等字段取 `ContextLen`，
但**上游两个域的模型端点都是 404**，从来不返回这些字段 → `ContextLen` 恒为 0 →
`listModels`（handler.go:282）仅当 `ContextLen > 0` 才输出该字段 → 字段整个消失。
所以**上游没有"档位"这个概念可查**：300k / 1M 是客户端侧的选择，网关完全看不到。

#### 实测二：模型实际能吃下 >1M（用真实请求压出来）

| 请求字符数 | 上游实收 prompt_tokens | 结果 |
|---|---|---|
| 1,320,000 | 270,018 | ✅ 正常返回 |
| 1,606,000 | 328,518 | ✅ 正常返回 |
| 4,224,000 | **864,018** | ✅ **正常返回** |

结论：**300k 档和 1M 档都不是硬墙**——至少到 864k 仍可正常响应。
所以"当前实际是 300k 还是 1M"这个问题，**答案是：网关不区分，模型侧上限远高于 300k**。
`[1M]` 这个数字虽然方向上不算错（确实 >1M），但它是**我编的**，不是你配置里读出来的。

#### 因此：我提交的 `[1M]` 改动是错的，应回退

- 已改：`wb-local.json` / `wb-nas.json` / 两个 `.example.json` 的 `ANTHROPIC_MODEL`
- **问题**：它把一个"用户可选档位"写死成了"1M 固有属性"，
  且没有任何数据源支撑——属于我凭推测改动用户配置，**违反了自己的判断标准**。
- **动作**：撤回这 4 处改动（见下）。**进度条要保持不显示**，
  直到有真实依据（比如你确认档位、或上游某天开始返回窗口字段）。

> 教训：`[1M]` 这类客户端语法只是"让 Claude Code 本地不再报错/按此分词"，
> **它不会改变上游行为**。写错不会报错、只会静默误导——比不写更危险。

#### ★ 那"原先的进度条"到底是哪来的？（2026-09-19 补充）

用户追问：既然网关不给窗口，那之前右下角那个 context 进度条是哪来的？

实测定位：**那个进度条来自 CC Switch，与本网关无关。**

```
$ ~/.claude/settings.json
ANTHROPIC_BASE_URL = http://127.0.0.1:15721        ← CC Switch，不是网关(7865)
ANTHROPIC_DEFAULT_OPUS_MODEL = claude-opus-5[1M]   ← 内置模型名，Claude Code 认窗口
ANTHROPIC_DEFAULT_OPUS_MODEL_NAME = deepseek-v4.1-flash
CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY = 1

$ netstat/进程 → 15721 = cc-switch.exe
```

关键差异是 **`ANTHROPIC_DEFAULT_*_MODEL` 的值**：

| 配置来源 | 该字段的值 | Claude Code 能否推出窗口 | 进度条 |
|---|---|---|---|
| CC Switch | `claude-opus-5[1M]` | ✅ 内置表里有 | ✅ 显示 |
| 本网关 profile | `deepseek-v4.1-flash[1M]` | ❌ 私有模型名，不在内置表 | ❌ 不显示 |

**所以进度条的分母是 Claude Code 自己按"它认识的模型名"算出来的**，
跟上游返回什么无关（上游 /v2/models 本来就是 404，从来没提供过窗口字段）。

**推论（重要）**：要让网关侧也显示进度条，唯一可行的办法是
**让 `ANTHROPIC_MODEL` 用一个 Claude Code 内置表认识的模型名**（如 `claude-opus-5[1M]`），
再靠 `..._MODEL_NAME` 映射成 `deepseek-v4.1-flash` 发给上游——
这正是 CC Switch 在做的事。**但这会改变客户端所见模型名**，要不要这么干得你定。

**用户已确认：WorkBuddy 客户端默认选的是 300k 档。** 因此：
- 网关侧 profile 保持**不写窗口后缀**（网关不知道档位，写死必然错）；
- 若将来要显示进度条，按上面推论的"内置模型名 + `_MODEL_NAME` 映射"方案做，
  而不是往 `ANTHROPIC_MODEL` 里塞 `[300k]`（那不是 Claude Code 的合法语法）。

#### ✅ 已实施：claude-* 别名映射（方案 A，提交 e8caa80）

按上面的推论落地了。实施中先撞到一堵墙：**光改 profile 会全线降级到混元。**

```
$ curl /v1/messages -d '{"model":"claude-opus-5[1M]",...}'
{"model":"hy4-preview", ...}      ← 裸名 claude-opus-5 也一样
```

原因在 `models.go:254` 的 `MapModelName`：判据是 `IsSupportedModel`，
而 `claude-*` 不在 `supportedModelSet` 里 → 一律回落 fallback
（**正是 profile 里 `//5`/`//6` 两条注释警告过的那个坑**）。
所以方案 A 必须**改代码**才成立。

改法：`MapModelName` 优先级改为 ①Claude 内置名 → 别名目标
（`claudeAliasTarget = "deepseek-v4.1-flash"`）②上游真实模型 → 透传
③其余 → fallback。别名置于最前——这类名字永远不可能是上游真实模型。
另加 `stripWindowSuffix` 剥掉 `[1M]`（`NormalizeModelName` 会保留方括号，
不清掉则前缀匹配和日志都难看）。

profile 改为（2 live + 2 example，与 CC Switch 写法对齐）：
`ANTHROPIC_MODEL` / `DEFAULT_*_MODEL` = 内置名（opus/sonnet/fable 用 `[1M]`，
haiku 用 `claude-haiku-4-5`），`_MODEL_NAME` 仍为 `deepseek-v4.1-flash`。

实测（容器已重建，三条协议全过）：

| 端点 | 传入 | 返回 model | 内容 |
|---|---|---|---|
| `/v1/messages` | `claude-opus-5[1M]` | `deepseek-v4.1-flash` | OK |
| `/v1/messages` | `claude-haiku-4-5` | `deepseek-v4.1-flash` | OK |
| `/v1/chat/completions` | `claude-opus-5[1M]` | `deepseek-v4.1-flash` | OK |
| `/v1/responses` | `claude-opus-5[1M]` | `deepseek-v4.1-flash` | OK |

上游真实模型名（`deepseek-v4.1-flash` / `deepseek-v4-pro` / `hy4-preview` /
`glm-5.3`）仍原样透传，未受影响。新增 2 个测试覆盖别名/透传/回落三类。

**副作用（已知并接受）**：客户端界面上显示的模型名会变成
`claude-opus-5[1M]` 而非 `deepseek-v4.1-flash`——这是拿到进度条的代价，
也是 CC Switch 一直在做的事。

**待办**：NAS 侧需自行 `./deploy.sh` 拉取本次提交后重建，profile 才生效。

> ⚠️ **订正**：上面这句是错的。**NAS 不能用 `deploy.sh`** ——
> `deploy.sh` 写死 `COMPOSE_FILE="docker-compose.server.yml"` 且按固定容器名
> `codebuddy2api` 工作，两者 NAS 都不适用（你自己的 `docs/deploy-nas.md:771`
> 已经写明这件事）。正确的 NAS 更新方式见下节。

#### ✅ NAS 更新包已备好（2026-09-19）

NAS 的 22 端口**当前不可达**（`Connection refused`），无法远程执行，
因此按 `docs/deploy-nas.md` 第七节的「打包传 NAS → 解包重启」流程在本机备好了包：
项目根目录 **`wb2api-src.tar.gz`**（已被 `.gitignore` 的 `*.tar.gz` 覆盖，不入库）。

**打包过程中踩到两个坑，都已修正：**

1. **不能用 `tar czf ... .` 打包** —— 它只按 `--exclude` 排除，
   而 `claude-profiles/wb-local.json` / `wb-nas.json`（**含真实网关 api_key**）
   是被 `.gitignore` 排除的、并非 `--exclude` 名单，
   于是**真实密钥被打进了包里**（已实测发现）。
   → 正确做法：**`git archive`**，它天然只导出被跟踪的文件、尊重 `.gitignore`：
   ```bash
   git archive --format=tar.gz -o /tmp/wb2api-src.tar.gz HEAD
   ```
2. 打包后仍需做 **CRLF→LF** 转换（Windows 换行会让 NAS 上的 `.sh` / compose 报出
   一串迷惑错误，见 `deploy-nas.md` 踩坑第 7 条）。

**NAS 上执行（`$PROJ` 换成实际路径，如 `/volume2/docker_ssd/workbuddy2api`）：**

```bash
cd "$PROJ"

# 0) 保险：更新前留一份当前镜像，便于回滚（见 deploy-nas.md 回滚·方式二）
sudo docker tag workbuddy2api-nas:latest workbuddy2api-nas:rollback-$(date +%F-%H%M)

# 1) 传包（22 不通时用 wget 反向拉，见 deploy-nas.md 踩坑清单）
#    本机先起个临时服务：python -m http.server 8000
#    NAS 上：
wget -O wb2api-src.tar.gz http://<本机IP>:8000/wb2api-src.tar.gz

# 2) 解包覆盖代码（不动 config.json / auths / data）
tar xzf wb2api-src.tar.gz && rm wb2api-src.tar.gz

# 3) 权限与配置自检（新文件权限会被重置，必须重跑）
chmod 755 . && chmod -R a+r .
python3 -m json.tool config.json > /dev/null && echo "✅ config.json 仍合法"

# 4) 重建并重启
sudo docker compose -f docker-compose.nas.yml up -d --build
sudo docker logs --tail 15 workbuddy2api-cb2api-1

# 5) 验证（应看到 accounts:1 healthy:1）
curl -s --noproxy '*' http://127.0.0.1:7865/healthz
```

**更新后 NAS 侧 profile 也要换**：包里的 `claude-profiles/*.json` **只是模板**
（`.example.json`，不含真 key）。NAS 上真正生效的 `wb-nas.json` 是**本地文件、不在包里**，
需手工把 `ANTHROPIC_MODEL` 等改成内置名（同本机 `wb-nas.json` 的写法），
或直接从本机拷一份过去（注意它含 NAS 网关的 key）。

**验收**：NAS 侧重启终端 + `claude --settings ...wb-nas.json`，
应能看到（a）上下文进度条回来；（b）对话正常、不是混元。





- [ ] 决策：是否在 `FallbackModels()` 的静态表里补 `ContextLen`
      （如统一填 200k/128k，或按模型分别标注）。**这是有副作用的改动**——
      填错会让 Claude Code 在超限前不预警，反而不如现在"不显示"安全。

### ✅ 已完成 — 待办 3：config.json

**核实结论：`config.json` 其实没有被 git 跟踪**（`git ls-files config.json` 为空，
`.gitignore` 已生效）。上一轮的判断有误，此条**无需处理**。

### `/admin` 鉴权：不做（用户决定）

理由（用户）：**本机的 wb2api 就没加**。两台实例行为保持一致，不加。
GitHub 上与该决定一致的既有做法：`/admin` 只部署在可信内网（本机 / NAS 内网），
README 第 228 行已有"置于可信网络或反代加鉴权"的提示。**此条关闭，不再跟催。**



---

## 附：一条环境事实（与上面三问都相关）

本机 `~/.claude/settings.json` 的 `env` 段把 `ANTHROPIC_BASE_URL` 指向
`http://127.0.0.1:15721`（**CC Switch**），且优先级高于进程环境变量。
因此**不带 `--settings` 启动时，请求走的是 CC Switch 而非本网关**。
`docs/usage.md` 已就此写过警告；上面 11148 与进度条两个问题都因此出现"两边不一致"的表象。
